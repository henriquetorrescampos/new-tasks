import { StyleSheet } from "react-native";

export default StyleSheet.create({
  noApplication: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 18,
    marginBottom: 20,
    marginTop: 20,
  },
});
