import { StyleSheet } from "react-native";

export default StyleSheet.create({
  noMessage: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 18,
    marginBottom: 20,
    marginTop: 20,
  },
});
